services:
  nutbits:
    build: .
    restart: always
    env_file: .env
    volumes:
      - nutbits-data:/data
    # No ports needed — communicates via Nostr relays only
    # CLI access: docker compose exec nutbits nutbits

volumes:
  nutbits-data:
